# Requirement Levels

Use MoSCoW-style levels:

- `must` — required for current stage exit criteria.
- `should` — important but can be delayed if needed.
- `could` — useful enhancement.
- `wont` — explicit non-goal for the current stage.

MVP must stay dominated by `must` requirements.
