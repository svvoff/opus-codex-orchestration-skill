# No Silent Downgrade

Switching from a primary orchestrator to a fallback orchestrator is a process change.

The user should be told when the switch happens, and the repository should record:

- current orchestrator mode;
- reason for fallback;
- restrictions now in force;
- what work can continue;
- what requires user or primary-orchestrator approval.
