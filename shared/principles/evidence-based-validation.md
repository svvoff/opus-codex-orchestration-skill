# Evidence-Based Validation

Codex cannot claim success without evidence.

A valid completion report must include:

- exact commands run;
- pass/fail result for each command;
- explanation for commands not run;
- evidence that acceptance criteria were checked;
- risk-specific validation notes;
- baseline failure assessment if anything failed.

Opus must not accept a task if validation evidence is missing.
