# Conservative Fallback Mode

Fallback orchestration should keep work moving without pretending to have the same authority as the primary orchestrator.

When in fallback mode:

- prefer low-risk ready tasks;
- avoid roadmap/architecture changes;
- do not weaken validation;
- ask for approval on high-risk tasks;
- read compact indexes first;
- record mode and restrictions in handoff state.
