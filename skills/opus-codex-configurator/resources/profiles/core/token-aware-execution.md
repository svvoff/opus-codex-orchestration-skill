# Profile: Token-Aware Execution

## Category

core/context-budget

## When to use

Always.

## Rules

- Read indexes first.
- Read only current/active files.
- Do not read archives by default.
- Keep `current-state.md`, `backlog/index.md`, and `execution-state.md` compact.
- Move completed tasks to `done/` and summarize them in compact ledgers.

## Opus review focus

Check whether the current execution consumed unnecessary context and improve indexes/task cards if needed.
